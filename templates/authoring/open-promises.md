# 开放承诺模板

> 使用方式：复制到 `stories/<故事名>/open-promises.md` 后填写。只记录读者已经能感知到的承诺或作者明确确认的长期承诺。

## 下一章推进优先级

| 优先级 | 承诺线 | 推进方式 | 边界 |
| --- | --- | --- | --- |
| P0 | `<下一章必须回应>` | `<通过行动、选择或后果推进>` | `<不提前兑现什么>` |
| P1 | `<可以轻触>` | `<一句话或小事件提示>` | `<不抢主线>` |
| P2 | `<暂缓>` | `<本章不推进>` | `<保持开放>` |

## 承诺线：`<名称>`

- 承诺：`<读者被文本或作者确认引导去期待什么>`
- 已建立：`<正文路径、章节、任务或确认记录>`
- 读者期待：`<下一次读者希望看到什么变化>`
- 下一次推进：`<近期可推进方式>`
- 边界提醒：`<不能突然解决、不能写成已发生、不能跳过代价>`
- 当前状态：`<open / touched / fulfilled / deferred>`

## 已兑现承诺

| 承诺 | 兑现位置 | 后续余波 |
| --- | --- | --- |
| `<承诺>` | `<正文路径>` | `<仍需追踪的影响>` |

## 不写入 canon 的候选

- `<候选想法>`：原因 `<未正文发生 / 作者未确认 / 只是 agent 建议>`。
