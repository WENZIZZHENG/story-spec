## 1. 插件安装安全

- [x] 1.1 冻结插件版本依赖和安装源解析契约。
- [x] 1.2 用 TDD 实现 `dependencies.core` 版本比较，不满足时阻止安装。
- [x] 1.3 用 TDD 实现插件源解析，支持内置插件、本地目录和 `file://` 本地 URL。

## 2. 世界观地点检查

- [x] 2.1 用脚本级测试复现未定义地点引用当前无法被发现的问题。
- [x] 2.2 实现 `check-world.sh` 的显式地点引用检查。
- [x] 2.3 同步生成的 agent 脚本产物和 manifest。

## 3. 验证与收尾

- [x] 3.1 运行相关单元测试和脚本测试。
- [x] 3.2 运行 OpenSpec 校验、构建、命令 manifest 检查和 smoke 测试。
- [x] 3.3 同步待办状态、changeset 和归档记录。
